# Copyright The Project Template Contributors

name: Build Dev Container Base Image

on:
  push:
    branches: [main]
    paths:
      - ".devcontainer/**"
      - ".github/workflows/docker.yml"
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  BASE_DEVCONTAINER_IMAGE: libodynamics/project_template/devcontainer

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v7
        with:
          context: .devcontainer
          file: .devcontainer/base.Dockerfile
          pull: true
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.BASE_DEVCONTAINER_IMAGE }}:latest
